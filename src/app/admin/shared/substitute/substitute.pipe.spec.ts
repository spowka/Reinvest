import { SubstitutePipe } from './substitute.pipe';

describe('SubstitutePipe', () => {
  it('create an instance', () => {
    const pipe = new SubstitutePipe();
    expect(pipe).toBeTruthy();
  });
});
