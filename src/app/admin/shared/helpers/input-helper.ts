export function keyDownAllowOnlyNumbers(event, maxLength: number = null) {
    // let maxLengthInvalid = maxLength ? event.target.value.length >= maxLength : false;

    // if (!event.ctrlKey && event.key != "Tab" && (event.key < '0' || event.key > '9' || maxLengthInvalid)) {
    //     event.preventDefault();
    // }
}